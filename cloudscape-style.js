// cloudscape-style.js
function aplicarCloudscapeDesign() {
  // Seleciona elementos e altera classes, atributos ou styles
  const botoes = document.querySelectorAll('button');
  botoes.forEach(btn => {// exemplo: adiciona classe do design system
    // ou alterar estilo diretamente:
    btn.style.backgroundColor = '#a0a0a0';
  });

  // Outras alterações visuais, atributos, etc.
}
